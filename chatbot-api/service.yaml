apiVersion: v1
kind: Service
metadata:
  name: k8s-chatbot-api-service
  namespace: website
  labels:
    app: k8s-chatbot-api
spec:
  selector:
    app: k8s-chatbot-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP 